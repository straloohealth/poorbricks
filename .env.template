# MongoDB credentials
# For local dev: use docker-compose up
MONGO_URI=mongodb://localhost:27017

# Contracts store
CONTRACTS_DB=metatron_contracts
CONTRACTS_COLLECTION=data_contracts

# For production: set MONGO_URI to the real MongoDB URI (via secrets manager or .env)
