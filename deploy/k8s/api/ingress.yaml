apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: poorbricks-server
  namespace: airflow
spec:
  ingressClassName: tailscale
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: poorbricks-server
                port:
                  number: 8080
